## AcWing





## leetcode

### [32. 最长有效括号](https://leetcode.cn/problems/longest-valid-parentheses/)

**初始化**：创建一个栈，并压入 `-1` 作为所有计算的初始基准。

**遍历字符串**：

- 如果遇到 `'('`，将它的索引压入栈。
- 如果遇到 `')'`：
  - 先弹出栈顶元素。
  - **如果弹出后栈为空**：说明当前的 `')'` 没有可匹配的 `'('`，它是一个无效的分割点。此时，将当前 `')'` 的索引压入栈，作为后续计算的新基准。
  - **如果弹出后栈不为空**：说明刚刚完成了一次成功的匹配。当前有效子串的长度就是 `当前索引 i - st.top()`（栈顶元素现在是这次匹配前的那个基准）。用这个长度更新最大值。

```c++
class Solution {
public:
    int longestValidParentheses(string s) {
        stack<int> st;
        st.push(-1);
        int ans = 0;
        int len = s.size();
        for(int i = 0; i < len; i ++) {
            if(s[i] == '(') {
                st.push(i);
            } else { // s[i] == ')'
                st.pop();
                if(st.empty()) {
                    st.push(i);
                } else {
                    ans = max(ans, i - st.top());
                }
            }
        }
        return ans;
    }
};
```



### [33. 搜索旋转排序数组](https://leetcode.cn/problems/search-in-rotated-sorted-array/)

`二分`

对于旋转数组 nums = [4,5,6,7,0,1,2]
首先根据 nums[0] 与 target 的关系判断 target 是在左段还是右段。

例如 target = 5, 目标值在左半段，因此在 [4, 5, 6, 7, inf, inf, inf] 这个有序数组里找就行了；
例如 target = 1, 目标值在右半段，因此在 [-inf, -inf, -inf, -inf, 0, 1, 2] 这个有序数组里找就行了。
如此，我们又双叒叕将「旋转数组中找目标值」 转化成了 「有序数组中找目标值」（妙哇！）

```c++
class Solution {
public:
    int search(vector<int>& nums, int target) {
        int left = 0, right = nums.size();
        int mid;

        while(left < right) {
            mid = (right - left) / 2 + left;
            if(nums[mid] == target) return mid;

            // target在左段
            if(nums[0] <= target) {
                if(nums[mid] < nums[0]) nums[mid] = INT_MAX; //mid在右
            } 
            // target在右段
            else {
                if(nums[mid] > nums[0]) nums[mid] = INT_MIN; //mid在左
            }

            if(nums[mid] < target) left = mid + 1;
            else right = mid; 
        }
        return -1;
    }
};
```



### [131. 分割回文串](https://leetcode.cn/problems/palindrome-partitioning/)

```c++
class Solution {
public:
    vector<vector<string> > ans;
    vector<string> path;
    int len;
    bool isPalindrome(string s, int start, int end) {
        while(start < end) {
            if(s[start++] != s[end--]) return false;
        }
        return true;
    }
    void dfs(string s, int i) {
        if(i == len) {
            ans.push_back(path);
            return;
        }
		
        for(int j = i; j < len; j ++) {
            // 如果是，则将其加入到路径中，如果不是，则直接跳过，尝试更长的子串
            if(isPalindrome(s, i, j)) {
                path.push_back(s.substr(i, j - i + 1));
                dfs(s, j + 1);// 递归：对剩余的字符串部分进行分割
                path.pop_back();// 回溯：撤销选择，将刚才加入的子串弹出
            }
        }

    }
    vector<vector<string>> partition(string s) {
        len = s.size();
        dfs(s, 0);
        return ans;
    }
};
```



### [153. 寻找旋转排序数组中的最小值](https://leetcode.cn/problems/find-minimum-in-rotated-sorted-array/)

`二分`

这道题的思路是利用旋转排序数组的特性，通过**二分搜索**来解决。在二分过程中，通过比较中间元素 `nums[mid]` 和右边界元素 `nums[right]` 的大小，我们可以判断出哪一部分是单调递增的，从而确定最小值所在的区间。如果 `nums[mid] < nums[right]`，则右半部分有序，最小值在左半部分或就是`mid`本身；反之，则左半部分有序，最小值在右半部分。通过不断缩小范围，最终定位到唯一的最小值，实现了 `O(log n)` 的时间复杂度。

```c++
class Solution {
public:
    int findMin(vector<int>& nums) {
        int left = 0, right = nums.size() - 1;
        int mid;
        while(left < right) {
            mid = (right - left) / 2 + left;
            if(nums[mid] > nums[right]) left = mid + 1; // mid-right 不是严格递增, 最小值在[mid+1, right]
            else right = mid; // nums[mid] < nums[right] 最小值在[left,mid]
        }
        return nums[left];
    }
};

```



### [200. 岛屿数量](https://leetcode.cn/problems/number-of-islands/)

搜索，从所有的1开始搜索，ans ++（1个1同样为一个岛屿），然后进行搜索，访问到的节点不为1时返回，已访问的节点将其值修改为'2'。

```c++
class Solution {
public:
    void dfs(vector<vector<char>>& grid, int x, int y) {
        int m = grid.size(), n = grid[0].size();
        if(x < 0 || x >= m || y < 0 || y >= n || grid[x][y] == '0' || grid[x][y] == '2') return;

        grid[x][y] = '2';
        dfs(grid, x - 1, y);
        dfs(grid, x + 1, y);
        dfs(grid, x, y - 1);
        dfs(grid, x, y + 1);
    }
    int numIslands(vector<vector<char>>& grid) {
        int m = grid.size(), n = grid[0].size();
        int ans = 0;

        for(int i = 0; i < m; i ++) {
            for(int j = 0; j < n; j ++) {
                if(grid[i][j] == '1') {
                    ans ++;
                    dfs(grid, i, j);
                }
            }
        }
        return ans;
    }
};
```

### [221. 最大正方形](https://leetcode.cn/problems/maximal-square/)

dp\[i]\[j] 表示以(i,j) 为右下角的所能形成的最大正方形边长

(i,j) 为 0，那么 dp(i,j) = 0

(i,j) 为 1，此时，以 `(i, j)` 为右下角可以形成一个至少边长为1的正方形。它能否形成更大的正方形，取决于它**左边、上边、和左上角**的三个邻居。因此：$dp[i][j] = min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1]) + 1$

```c++
class Solution {
public:
    int maximalSquare(vector<vector<char>>& matrix) {
        int m = matrix.size(), n = matrix[0].size();
        vector<vector<int> > dp(m + 1, vector<int> (n + 1, 0));
        int maxside = 0;
        for(int i = 0; i < m; i ++) {
            for(int j = 0; j < n; j ++) {
                if(matrix[i][j] == '1') dp[i + 1][j + 1] = min{dp[i][j], dp[i + 1][j], dp[i][j + 1]} + 1;

                maxside = max(maxside, dp[i][j]);
            }
        }
        return maxside * maxside;
    }
};
```



### [1312. 让字符串成为回文串的最少插入次数](https://leetcode.cn/problems/minimum-insertion-steps-to-make-a-string-palindrome/)

**思路1：**

换个角度想:当前字符串要变成回文,那只要把不一样的找出来就好了.即:求出反过来的字符串和当前字符串的最长公共子序列,然后减一下

```html
如 : "ab" --> reverse  ---> "ba",
"ab"和"ba"的 最长公共子序列长度是 1, 即 len("ab") = 2,  2 - 1 = 1, 即 只要插入1个字母就好咯. "aba" 或者"bab".
如 leetcode 和 
     edocteel 的最长公共子序列是  e t e ,长度为3,  即 8-3 = 5.
把公共部分保持不变,其他的插过来,或者插过去,就一样啦!
```



```c++
class Solution {
public:
    int minInsertions(string s) {
        int n = s.size();
        string rs = s;
        reverse(rs.begin(), rs.end());
        vector<vector<int> > dp(n + 1, vector<int>(n + 1, 0));
        for(int i = 0; i < n; i ++) {
            for(int j = 0; j < n; j ++) {
                if(s[i] == rs[j]) dp[i+1][j+1] = dp[i][j] + 1;
                else dp[i+1][j+1] = max(dp[i+1][j], dp[i][j+1]);
            }
        }
        return n - dp[n][n];
    }
};
```

