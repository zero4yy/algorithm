#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N = 2e5 + 5;
ll n, c;
ll ans = 0;
map<ll,ll> m;
ll a[N];
int main(){
	 //cin >> n >> c;
	// for(int i = 0;i < n; i++){
		// cin >> a;
		// if(m.find(a) == m.end())
			// m[a] = 1;
		// else 
			// m[a] ++;
	// }
	// int ans = 0;
	// for(auto & it : m){
		// if(m.find(it.first + c) != m.end())
			// ans += (it.second) * m[it.first + c]; 
		// //cout << ans << endl;
	// }
	// //cout << m[1] << m[2] << m[3] <<endl;
	// cout << ans;
	// for(int i = 0;i < n; i++){
	// 	cin >> a[i];
	// 	m[a[i]]++;
	// }
	// for(int i = 0;i < n; i++){
	// 	ans += m[a[i]-c];
	// }
	// cout << ans;
	cout << "hello world";
}